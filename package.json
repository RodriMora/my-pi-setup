{
  "name": "my-pi-setup",
  "version": "0.1.0",
  "private": true,
  "description": "Rodri's personal pi setup: extensions, skills, and themes packaged for sharing",
  "keywords": [
    "pi-package"
  ],
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "echo 'nothing to build'"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "skills": [
      "./skills"
    ],
    "themes": [
      "./themes"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "dependencies": {
    "@effect/platform-node": "^4.0.0-beta.99",
    "effect": "^4.0.0-beta.99"
  },
  "devDependencies": {
    "@effect/vitest": "^4.0.0-rc.109",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  }
}
